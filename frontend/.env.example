# Environment variables.

# Port number.
PORT=3100

# Backend API base URL (must end with /v1/). Injected into config.js for the browser.
API_BASE=http://localhost:3000/v1/

# The Cat API key. Used by the browser via /assets/js/config.js.
CAT_API_KEY=